name: pwmetrics-performance-test

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  performance:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3

      - name: 🧰 Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: 📦 Install dependencies
        run: |
          npm install -g pwmetrics
          npm install

      - name: 🚀 Launch Chrome manually in headless mode
        run: |
          google-chrome --headless --remote-debugging-port=9222 --no-sandbox --disable-gpu &

      - name: 🧪 Run pwmetrics with Chrome debugging port
        run: |
          # Wait for Chrome to be ready
          sleep 5
          pwmetrics https://playwright.com --json --port=9222 > performance-results.json
        continue-on-error: true

      - name: 📤 Upload results
        uses: actions/upload-artifact@v4
        with:
          name: pwmetrics-results
          path: performance-results.json
