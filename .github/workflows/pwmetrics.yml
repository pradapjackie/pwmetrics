name: pwmetrics-performance-test

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  performance:
    runs-on: ubuntu-latest

    steps:
      - name: 🧰 Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: 📦 Install pwmetrics
        run: npm install -g pwmetrics

      - name: 🐳 Start Chrome with remote debugging
        run: |
          docker run -d \
            --name chrome-debug \
            -p 9222:9222 \
            --shm-size=2gb \
            ghcr.io/browserless/chrome:latest \
            chrome \
            --headless=new \
            --no-sandbox \
            --disable-gpu \
            --remote-debugging-address=0.0.0.0 \
            --remote-debugging-port=9222 \
            about:blank

          echo "🕒 Waiting for Chrome..."
          sleep 10

      - name: 🧪 Confirm Chrome is ready
        run: curl http://localhost:9222/json/version

      - name: 📊 Run pwmetrics on playwright.com
        run: |
          pwmetrics https://playwright.com \
            --json \
            --port=9222 > performance-results.json

      - name: 📤 Upload results
        uses: actions/upload-artifact@v4
        with:
          name: pwmetrics-results
          path: performance-results.json

      - name: 🧹 Stop and remove Chrome container
        run: docker rm -f chrome-debug
